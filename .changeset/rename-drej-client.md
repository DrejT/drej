---
"drej": minor
---

Rename `DrejClient` → `Drej` and `DrejClientOptions` → `DrejOptions`. Add `WorkflowRun.stdout()`, `WorkflowRun.result()`, and `WorkflowRun.pipe()` for ergonomic output consumption without manual event filtering.
