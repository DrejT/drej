---
---

<section class="claim">
  <div class="inner">
    <p class="statement">
      Not a copy. Not a re-run. <span class="accent">The exact checkpointed
      state</span>, forked.
    </p>
  </div>
</section>

<style>
  .claim {
    padding: 5rem 2rem;
    border-top: 1px solid var(--color-border);
  }

  .inner {
    max-width: 700px;
    margin: 0 auto;
    text-align: center;
  }

  .statement {
    font-size: clamp(1.5rem, 3.6vw, 2.25rem);
    font-weight: 500;
    letter-spacing: -0.015em;
    line-height: 1.3;
    color: var(--color-muted);
  }

  .accent {
    color: var(--color-fg);
    font-weight: 600;
  }
</style>
