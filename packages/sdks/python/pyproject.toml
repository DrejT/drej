[project]
name = "drej"
version = "0.1.0"
description = "Python SDK for the drej orchestration API"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
