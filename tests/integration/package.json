{
  "name": "@drej/integration-tests",
  "private": true,
  "version": "0.0.4",
  "scripts": {
    "test": "bun test --timeout 120000"
  },
  "dependencies": {
    "@drej/agent": "workspace:*",
    "@drej/sqlite": "workspace:*",
    "drej": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
